import { SimpleToolPage } from '@/components/SimpleToolPage'

export function PageNumbers() {
  return (
    <SimpleToolPage
      tool="page-numbers"
      title="Add Page Numbers"
      description="Number every page automatically, bottom-center."
      submitLabel="Add Page Numbers"
    />
  )
}
