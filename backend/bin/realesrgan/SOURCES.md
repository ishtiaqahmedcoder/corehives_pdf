`realesrgan-ncnn-vulkan.exe` and the bundled `models/realesrgan-x4plus.{bin,param}` are from [xinntao/Real-ESRGAN](https://github.com/xinntao/Real-ESRGAN), release [v0.2.5.0](https://github.com/xinntao/Real-ESRGAN/releases/tag/v0.2.5.0) (`realesrgan-ncnn-vulkan-20220424-windows.zip`), licensed under the BSD 3-Clause License (free, including commercial use). Only the general-purpose `realesrgan-x4plus` model is kept here (the anime-specific and video models from the same release were removed to keep this smaller, since this project's Upscale Image tool is for general photos).

Used by `App\Services\Image\UpscaleImageService` via `Illuminate\Support\Facades\Process`, same pattern as the other CLI-binary tools (Ghostscript, qpdf, Tesseract, LibreOffice, wkhtmltoimage) in `config/pdftools.php`.
